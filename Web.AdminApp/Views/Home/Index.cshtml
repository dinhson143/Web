@model Web.Models.ProductSavestModel
@{
    ViewData["Title"] = "Trang chủ";
    var month = DateTime.Now.Month;
    var date = DateTime.Now.Day + "/" + DateTime.Now.Month + "/" + DateTime.Now.Year;
    var From = Model.From.Day + "/" + Model.From.Month + "/" + Model.From.Year;
    var To = Model.To.Day + "/" + Model.To.Month + "/" + Model.To.Year;
    var doanhthu = Model.listDT;
    var OD = Model.listSLOD;
    var loinhuanthangnay = Model.listDT[month - 1].ToString("N0");
    var SLODthangnay = Model.listSLOD[month - 1];
    var listUS = Model.listUS;
    decimal doanhthuFromTo = 0;
    var soluongdonhangFT = -1;
    if (Model.listDoanhthuFromTo != null)
    {
        soluongdonhangFT = Model.listDoanhthuFromTo.Count;
        foreach (var item in Model.listDoanhthuFromTo)
        {
            doanhthuFromTo += (item.TongtienReal - item.Tongtien);
        }
    }
    var dem = 0;
}

@section Scripts{
    <!-- Page level plugins -->
    <script src="/vendor/chart.js/Chart.min.js"></script>
    <script src="/js/demo/chart-area-demo.js"></script>
    <script src="/js/demo/chart-pie-demo.js"></script>
    @*biểu đồ tháng*@
    <script>
        // biểu đồ tháng
        var array = JSON.parse('@Newtonsoft.Json.JsonConvert.SerializeObject(doanhthu)'.replace(/&quot;/g, "\""));
        var OD = JSON.parse('@Newtonsoft.Json.JsonConvert.SerializeObject(OD)'.replace(/&quot;/g, "\""));
        var ctx = document.getElementById("myAreaChart");
        var myLineChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
                datasets: [{
                    label: "Doanh thu",
                    lineTension: 0.3,
                    backgroundColor: "rgba(78, 115, 223, 0.05)",
                    borderColor: "rgba(78, 115, 223, 1)",
                    pointRadius: 3,
                    pointBackgroundColor: "rgba(78, 115, 223, 1)",
                    pointBorderColor: "rgba(78, 115, 223, 1)",
                    pointHoverRadius: 3,
                    pointHoverBackgroundColor: "rgba(78, 115, 223, 1)",
                    pointHoverBorderColor: "rgba(78, 115, 223, 1)",
                    pointHitRadius: 10,
                    pointBorderWidth: 2,
                    data: array,
                }],
            },
            options: {
                maintainAspectRatio: false,
                layout: {
                    padding: {
                        left: 10,
                        right: 25,
                        top: 25,
                        bottom: 0
                    }
                },
                scales: {
                    xAxes: [{
                        time: {
                            unit: 'date'
                        },
                        gridLines: {
                            display: false,
                            drawBorder: false
                        },
                        ticks: {
                            maxTicksLimit: 7
                        }
                    }],
                    yAxes: [{
                        ticks: {
                            maxTicksLimit: 5,
                            padding: 10,
                            // Include a dollar sign in the ticks
                            callback: function (value, index, values) {
                                return '$' + number_format(value);
                            }
                        },
                        gridLines: {
                            color: "rgb(234, 236, 244)",
                            zeroLineColor: "rgb(234, 236, 244)",
                            drawBorder: false,
                            borderDash: [2],
                            zeroLineBorderDash: [2]
                        }
                    }],
                },
                legend: {
                    display: false
                },
                tooltips: {
                    backgroundColor: "rgb(255,255,255)",
                    bodyFontColor: "#858796",
                    titleMarginBottom: 10,
                    titleFontColor: '#6e707e',
                    titleFontSize: 14,
                    borderColor: '#dddfeb',
                    borderWidth: 1,
                    xPadding: 15,
                    yPadding: 15,
                    displayColors: false,
                    intersect: false,
                    mode: 'index',
                    caretPadding: 10,
                    callbacks: {
                        label: function (tooltipItem, chart) {
                            var datasetLabel = chart.datasets[tooltipItem.datasetIndex].label || '';
                            return datasetLabel + ': $' + number_format(tooltipItem.yLabel);
                        }
                    }
                }
            }
        });
        function doanhthu() {
            myLineChart.destroy();
            myLineChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
                    datasets: [{
                        label: "Doanh thu",
                        lineTension: 0.3,
                        backgroundColor: "rgba(78, 115, 223, 0.05)",
                        borderColor: "rgba(78, 115, 223, 1)",
                        pointRadius: 3,
                        pointBackgroundColor: "rgba(78, 115, 223, 1)",
                        pointBorderColor: "rgba(78, 115, 223, 1)",
                        pointHoverRadius: 3,
                        pointHoverBackgroundColor: "rgba(78, 115, 223, 1)",
                        pointHoverBorderColor: "rgba(78, 115, 223, 1)",
                        pointHitRadius: 10,
                        pointBorderWidth: 2,
                        data: array,
                    }],
                },
                options: {
                    maintainAspectRatio: false,
                    layout: {
                        padding: {
                            left: 10,
                            right: 25,
                            top: 25,
                            bottom: 0
                        }
                    },
                    scales: {
                        xAxes: [{
                            time: {
                                unit: 'date'
                            },
                            gridLines: {
                                display: false,
                                drawBorder: false
                            },
                            ticks: {
                                maxTicksLimit: 7
                            }
                        }],
                        yAxes: [{
                            ticks: {
                                maxTicksLimit: 5,
                                padding: 10,
                                // Include a dollar sign in the ticks
                                callback: function (value, index, values) {
                                    return '$' + number_format(value);
                                }
                            },
                            gridLines: {
                                color: "rgb(234, 236, 244)",
                                zeroLineColor: "rgb(234, 236, 244)",
                                drawBorder: false,
                                borderDash: [2],
                                zeroLineBorderDash: [2]
                            }
                        }],
                    },
                    legend: {
                        display: false
                    },
                    tooltips: {
                        backgroundColor: "rgb(255,255,255)",
                        bodyFontColor: "#858796",
                        titleMarginBottom: 10,
                        titleFontColor: '#6e707e',
                        titleFontSize: 14,
                        borderColor: '#dddfeb',
                        borderWidth: 1,
                        xPadding: 15,
                        yPadding: 15,
                        displayColors: false,
                        intersect: false,
                        mode: 'index',
                        caretPadding: 10,
                        callbacks: {
                            label: function (tooltipItem, chart) {
                                var datasetLabel = chart.datasets[tooltipItem.datasetIndex].label || '';
                                return datasetLabel + ': $' + number_format(tooltipItem.yLabel);
                            }
                        }
                    }
                }
            });
        }
        function donhangMonth() {
            myLineChart.destroy();
            var ctx = document.getElementById("myAreaChart");
            myLineChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
                    datasets: [{
                        label: "Số lượng",
                        lineTension: 0.3,
                        backgroundColor: "rgba(78, 115, 223, 0.05)",
                        borderColor: "rgba(78, 115, 223, 1)",
                        pointRadius: 3,
                        pointBackgroundColor: "rgba(78, 115, 223, 1)",
                        pointBorderColor: "rgba(78, 115, 223, 1)",
                        pointHoverRadius: 3,
                        pointHoverBackgroundColor: "rgba(78, 115, 223, 1)",
                        pointHoverBorderColor: "rgba(78, 115, 223, 1)",
                        pointHitRadius: 10,
                        pointBorderWidth: 2,
                        data: OD,
                    }],
                },
                options: {
                    maintainAspectRatio: false,
                    layout: {
                        padding: {
                            left: 10,
                            right: 25,
                            top: 25,
                            bottom: 0
                        }
                    },
                    scales: {
                        xAxes: [{
                            time: {
                                unit: 'date'
                            },
                            gridLines: {
                                display: false,
                                drawBorder: false
                            },
                            ticks: {
                                maxTicksLimit: 7
                            }
                        }],
                        yAxes: [{
                            ticks: {
                                maxTicksLimit: 5,
                                padding: 10,
                                // Include a dollar sign in the ticks
                                callback: function (value, index, values) {
                                    return '' + number_format(value);
                                }
                            },
                            gridLines: {
                                color: "rgb(234, 236, 244)",
                                zeroLineColor: "rgb(234, 236, 244)",
                                drawBorder: false,
                                borderDash: [2],
                                zeroLineBorderDash: [2]
                            }
                        }],
                    },
                    legend: {
                        display: false
                    },
                    tooltips: {
                        backgroundColor: "rgb(255,255,255)",
                        bodyFontColor: "#858796",
                        titleMarginBottom: 10,
                        titleFontColor: '#6e707e',
                        titleFontSize: 14,
                        borderColor: '#dddfeb',
                        borderWidth: 1,
                        xPadding: 15,
                        yPadding: 15,
                        displayColors: false,
                        intersect: false,
                        mode: 'index',
                        caretPadding: 10,
                        callbacks: {
                            label: function (tooltipItem, chart) {
                                var datasetLabel = chart.datasets[tooltipItem.datasetIndex].label || '';
                                return datasetLabel + ':' + number_format(tooltipItem.yLabel);
                            }
                        }
                    }
                }
            });
        }
    </script>
    @*biểu đồ tròn*@
    <script>
        var US = JSON.parse('@Newtonsoft.Json.JsonConvert.SerializeObject(listUS)'.replace(/&quot;/g, "\""));
        var ctx2 = document.getElementById("myPieChart");
        var myPieChart = new Chart(ctx2, {
            type: 'doughnut',
            data: {
                labels: ["TV mới", "TV đồng", "TV bạc", "TV vàng"],
                datasets: [{
                    data: US,
                    backgroundColor: ['#4e73df', '#1cc88a', '#36b9cc', '#e74a3b'],
                    hoverBackgroundColor: ['#2e59d9', '#17a673', '#2c9faf', '#e74a3b'],
                    hoverBorderColor: "rgba(234, 236, 244, 1)",
                }],
            },
            options: {
                maintainAspectRatio: false,
                tooltips: {
                    backgroundColor: "rgb(255,255,255)",
                    bodyFontColor: "#858796",
                    borderColor: '#dddfeb',
                    borderWidth: 1,
                    xPadding: 15,
                    yPadding: 15,
                    displayColors: false,
                    caretPadding: 10,
                },
                legend: {
                    display: false
                },
                cutoutPercentage: 80,
            },
        });
    </script>
    @*validate ngày*@
    <script>
        function btnXemClick() {
            //alert("aaa");
            var dateF = new Date($('#dateFrom').val());
            var dateT = new Date($('#dateTo').val());
            var day = dateF.getDate();
            var month = dateF.getMonth() + 1;
            var year = dateF.getFullYear();
            var dateFrom = [month, day, year].join('-');
            //
            var day2 = dateT.getDate();
            var month2 = dateT.getMonth() + 1;
            var year2 = dateT.getFullYear();
            var dateTo = [month2, day2, year2].join('-');
            if (isNaN(day) == true || isNaN(day2) == true) {
                $('.noteMes').text("Vui lòng chọn ngày");
                return false;
            }
            if (dateF.getTime() > dateT.getTime()) {
                $('.noteMes').text("Ngày không hợp lệ");
                return false;
            }
        }
    </script>
}
@*Page Heading*@
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Bảng thống kê</h1>
</div>
<form method="post" asp-action="Index" asp-controller="Home" style="text-align:center">
    <label style="margin:0 5px">From</label>
    <input asp-for="From" type="date" id="dateFrom">
    <label style="margin:0 5px">To</label>
    <input asp-for="To" type="date" id="dateTo">

    <button type="submit" onclick="return btnXemClick()" class="btn btn-primary btnXem">Xem</button>
</form>
<p class="noteMes" style="text-align: center; color: red; background-color: cornsilk;"></p>
<!-- Content Row -->
<div class="row">

    <!-- Earnings (Monthly) Card Example -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-primary shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                            @if (doanhthuFromTo >= 0 && From != To)
                            {
                                <p>Số lượng đơn hàng từ @From tới @To</p>
                            }
                            else
                            {
                                <p>Số lượng đơn hàng tháng @month</p>
                            }
                        </div>
                        @if (soluongdonhangFT >= 0 )
                        {
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@soluongdonhangFT đơn hàng</div>
                        }
                        else
                        {
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@SLODthangnay đơn hàng</div>
                        }
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-calendar fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Earnings (Monthly) Card Example loi nhuan -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-success shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                            @if (doanhthuFromTo >= 0 && From != To)
                            {
                                <p>Doanh thu từ @From tới @To</p>
                            }
                            else
                            {
                                <p>Doanh thu tháng @month</p>
                            }
                        </div>
                        @if (doanhthuFromTo >= 0)
                        {
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@doanhthuFromTo.ToString("N0") VND</div>
                        }
                        else
                        {
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@loinhuanthangnay VND</div>
                        }
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-dollar-sign fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Earnings (Monthly) Card Example -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-info shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                            Đơn hàng chờ xác nhận
                        </div>
                        <div class="row no-gutters align-items-center">
                            <div class="col-auto">
                                <div class="h5 mb-0 mr-3 font-weight-bold text-gray-800">@Model.listOdIPro.Count</div>
                            </div>
                            <div class="col">
                                <div class="progress progress-sm mr-2">
                                    <div class="progress-bar bg-info" role="progressbar"
                                         style="width: 50%" aria-valuenow="50" aria-valuemin="0"
                                         aria-valuemax="100"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-clipboard-list fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Pending Requests Card Example -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-warning shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                            Bình luận mới ngày @date
                        </div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">@Model.listCM.Count </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-comments fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Content Row -->

<div class="row">

    <!-- Area Chart biểu đồ tháng-->
    <div class="col-xl-8 col-lg-7">
        <div class="card shadow mb-4">
            <!-- Card Header - Dropdown -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Biểu đồ tháng</h6>
                <div class="dropdown no-arrow">
                    <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in"
                         aria-labelledby="dropdownMenuLink">
                        <div class="dropdown-header">Chọn loại thống kê:</div>
                        <a class="dropdown-item" onclick="return doanhthu()">Doanh thu</a>
                        <a class="dropdown-item" onclick="return donhangMonth()">Đơn hàng</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>
                    </div>
                </div>
            </div>
            <!-- Card Body -->
            <div class="card-body">
                <div class="chart-area">
                    <canvas id="myAreaChart"></canvas>
                </div>
            </div>
        </div>
    </div>

    <!-- Pie Chart thống kê thành viên-->
    <div class="col-xl-4 col-lg-5">
        <div class="card shadow mb-4">
            <!-- Card Header - Dropdown -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Thống kê thành viên</h6>
                <div class="dropdown no-arrow">
                    <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in"
                         aria-labelledby="dropdownMenuLink">
                        <div class="dropdown-header">Dropdown Header:</div>
                        <a class="dropdown-item" href="#">Action</a>
                        <a class="dropdown-item" href="#">Another action</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>
                    </div>
                </div>
            </div>
            <!-- Card Body -->
            <div class="card-body">
                <div class="chart-pie pt-4 pb-2">
                    <canvas id="myPieChart"></canvas>
                </div>
                <div class="mt-4 text-center small">
                    <span class="mr-2">
                        <i class="fas fa-circle text-primary"></i> TV mới
                    </span>
                    <span class="mr-2">
                        <i class="fas fa-circle text-success"></i> TV đồng
                    </span>
                    <span class="mr-2">
                        <i class="fas fa-circle text-info"></i> TV bạc
                    </span>
                    <span class="mr-2">
                        <i class="fas fa-circle text-danger"></i> TV vàng
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Content Row  sản phẩm bán chạy nhất-->

<div class="row">

    <!-- Content Column -->
    <div class="col-lg-6 mb-4">

        <!-- Project Card Example -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                @{
                    var from = Model.From.Day + "/" + Model.From.Month + "/" + Model.From.Year;
                    var to = @Model.To.Day + "/" + @Model.To.Month + "/" + @Model.To.Year;
                }
                @if (From != To)
                {
                    <h6 class="m-0 font-weight-bold text-primary">5 Sản phẩm bán chạy nhất từ @from tới @to</h6>
                }
                else
                {
                    <h6 class="m-0 font-weight-bold text-primary">5 Sản phẩm bán chạy nhất ngày @from</h6>
                }
            </div>
            <div class="card-body">
                <h4 class="small font-weight-bold">
                    @if (Model.listPro.Count > 4)
                    {
                        @Model.listPro[4].Name <span class="float-right">@Model.listPro[4].SluongDaban sản phẩm</span>
                    }
                    else
                    {
                        <span>Tên sản phẩm</span><span class="float-right">0 sản phẩm</span>
                    }
                </h4>
                <div class="progress mb-4">
                    <div class="progress-bar bg-danger" role="progressbar" style="width: 20%"
                         aria-valuenow="20" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
                <h4 class="small font-weight-bold">
                    @if (Model.listPro.Count > 3)
                    {
                        @Model.listPro[3].Name <span class="float-right">@Model.listPro[3].SluongDaban sản phẩm</span>
                    }
                    else
                    {
                        <span>Tên sản phẩm</span><span class="float-right">0 sản phẩm</span>
                    }
                </h4>
                <div class="progress mb-4">
                    <div class="progress-bar bg-warning" role="progressbar" style="width: 40%"
                         aria-valuenow="40" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
                <h4 class="small font-weight-bold">
                    @if (Model.listPro.Count > 2)
                    {
                        @Model.listPro[2].Name <span class="float-right">@Model.listPro[2].SluongDaban sản phẩm</span>
                    }
                    else
                    {
                        <span>Tên sản phẩm</span><span class="float-right">0 sản phẩm</span>
                    }
                </h4>
                <div class="progress mb-4">
                    <div class="progress-bar" role="progressbar" style="width: 60%"
                         aria-valuenow="60" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
                <h4 class="small font-weight-bold">
                    @if (Model.listPro.Count > 1)
                    {
                        @Model.listPro[1].Name <span class="float-right">@Model.listPro[1].SluongDaban sản phẩm</span>
                    }
                    else
                    {
                        <span>Tên sản phẩm</span><span class="float-right">0 sản phẩm</span>
                    }
                </h4>
                <div class="progress mb-4">
                    <div class="progress-bar bg-info" role="progressbar" style="width: 80%"
                         aria-valuenow="80" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
                <h4 class="small font-weight-bold">
                    @if (Model.listPro.Count > 0)
                    {
                        @Model.listPro[0].Name <span class="float-right">@Model.listPro[0].SluongDaban sản phẩm</span>
                    }
                    else
                    {
                        <span>Tên sản phẩm</span><span class="float-right">0 sản phẩm</span>
                    }
                </h4>
                <div class="progress">
                    <div class="progress-bar bg-success" role="progressbar" style="width: 100%"
                         aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
            </div>
        </div>
    </div>
    @*sản phẩm yêu thích nhất*@
    <div class="card shadow col-lg-6 mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">
                Top sản phẩm được yêu thích nhất
            </h6>
        </div>
        <div class="card-body">
            @foreach (var item in Model.listProSalest)
            {
                dem++;
                <div class="p-3 bg-gray-600 text-white">@dem:  @item.Name</div>
            }
            
        </div>
    </div>
</div>
